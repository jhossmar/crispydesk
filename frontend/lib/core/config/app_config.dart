/// Backend base URL. Assumes the backend runs on the same machine as the
/// browser (web) or desktop app during development. If you later run this
/// on an Android emulator, that needs 10.0.2.2 instead of localhost.
const String backendBaseUrl = 'http://localhost:3000';
